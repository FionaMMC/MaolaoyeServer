# QMT 模拟盘 Pipeline — 项目设计文档（纯股）v2.1

**版本**：v2.1  
**日期**：2026-04-28  
**状态**：规划阶段，与搭档评审后可开发  
**范围**：仅股票，支持多账户、多策略、信号归集与虚拟资金隔离

---

## 与 v1.0 的主要变化

| 变化点 | v1.0 旧设计 | v2.1 设计 | 原因 |
|--------|-----------|---------|------|
| 账户支持 | 单账户 | 多账户（account_group 概念） | 支持多策略并行并独立归因 |
| 成交回报推送次数 | 两次（09:35 竞价 + 15:30 收盘） | 仅一次（15:30 收盘） | 09:35 竞价回报仅用于人工监控，服务器不需要；简化流程 |
| 账户状态推送 | 09:05 推送 POST /account-state | 完全删除 | 服务器不维护真实账户状态，风控下放到本地 |
| 策略持仓感知 | 服务器维护真实持仓 | 服务器只维护虚拟持仓（由成交回报更新） | 策略所需持仓即虚拟持仓，真实账户由本地管理 |
| 数据存储 | 按日期分区 | 按标的存储（Parquet 文件） | 策略获取单股历史序列更高效 |
| 信号粒度 | 信号 = 订单（一对一） | 信号 → 预检查 → 归集 → 订单（多对一） | 节省手续费，实现策略资金隔离 |
| 本地风控 | 无 | 本地 09:15 前实时查询 QMT 真实资金/持仓 | 更准确，无网络延迟 |
| 行情推送内容 | 仅股票日线 | 股票 + 指数 + ETF 日线 | 策略依赖指数和 ETF 数据 |
| 行情推送时间 | 15:30 | 15:35 | 避免与成交回报推送冲突，行情稳定后再下载 |
| 查询接口 | GET /signals | GET /orders | 本地只需执行归集后的订单，无需知道原始信号 |
| 本地 SQLite 表命名 | signals / orders | server_orders / local_orders | 消除歧义：server_orders 存服务器下发的订单，local_orders 存 QMT 委托记录 |

---

## 项目目标

建立一套自动化交易信号执行管道，支持：

- 多策略共享同一真实账户，各自拥有独立虚拟资金池和绩效记录
- 信号归集（合并同标的同方向订单）以节省手续费
- 虚拟资金隔离：归集前按策略虚拟资金/持仓预检查
- 完整绩效归因：每个策略实例独立净值跟踪
- 极简网络交互：每日仅两次推送（15:30 成交回报 + 15:35 行情）+ 一次拉取（19:00 订单）

---

## 核心概念

### account_group 与策略实例

`account_group` 是一个真实 QMT 账户的标识符。每个 `account_group` 下可以配置多个策略，每个策略在服务器上对应一个独立的策略实例（instance），实例 ID 格式为 `{account_group}_{strategy_id}`。

本地通过配置文件 `strategies.yaml` 建立 `account_group` 到 QMT 真实账户 ID 的映射。下单时，本地根据订单的 `account_group` 字段查找对应的 `qmt_account_id`，再调用 QMT API。

配置示例：

```yaml
account_groups:
  - group_id: real_A
    qmt_account_id: "1234567890"   # QMT 中的真实证券账号
    strategies:
      - strategy_id: momentum
        virtual_initial_cash: 500000   # 虚拟初始资金（元）
      - strategy_id: mean_reversion
        virtual_initial_cash: 300000
  - group_id: real_B
    qmt_account_id: "0987654321"
    strategies:
      - strategy_id: momentum
        virtual_initial_cash: 1000000
```

策略实例示例：`real_A_momentum`、`real_A_mean_reversion`、`real_B_momentum`。

### 虚拟资金隔离

服务器为每个策略实例维护独立虚拟账本：

- **virtual_cash**：该策略可用于买入的资金（元）
- **virtual_positions**：该策略持有的股票及数量

成交回报推送后，服务器根据 order→signal 映射表，将实际成交按比例拆分到各原始信号，更新对应实例的虚拟账本。绩效净值 = 虚拟现金 + Σ(虚拟持仓 × 当日收盘价)。

### 归集引擎

同一 `account_group` 下，若多个策略实例在同一交易日对同一标的有相同方向的信号，归集引擎将它们合并为一条真实订单。合并规则：

- **quantity**：各信号数量求和
- **limit_price**：取所有信号中最激进的价格
  - BUY 方向：取 max(limit_price)，出价最高，最大化成交概率
  - SELL 方向：取 min(limit_price)，要价最低，最大化成交概率

归集后记录 order→signal 映射及各信号数量，供后续按比例拆分成交时使用。

### A 股手数规则

- **买入**：最小 1 手 = 100 股，数量必须为 100 的整数倍
- **卖出**：通常为 100 的整数倍；当策略虚拟持仓剩余不足 100 股时，允许卖出全部剩余持仓（尾单清仓）

策略引擎负责在输出信号时遵守此规则；预检查模块作为安全网，对不合规信号标记 FAIL 并告警。

---

## 系统架构

```
【本地 QMT + Python】                          【服务器 Linux Ubuntu】
      │                                                    │
每日 15:30 查询全天成交 ──────────────────────→ POST /trade-result
      │                                                    ↓
每日 15:35 下载行情并推送 ─────────────────────→ POST /market-data
（股票 + 指数 + ETF）                                       ↓
      │                                       行情入库（按标的 Parquet）
      │                                                    ↓
      │                                  根据成交回报更新虚拟资金/持仓
      │                                                    ↓
      │                                         触发策略引擎（多实例）
      │                                         ├─ real_A_momentum
      │                                         ├─ real_A_mean_reversion
      │                                         └─ real_B_momentum
      │                                                    ↓
      │                              原始信号 → 预检查 → 归集 → 订单队列
      │                                                    ↓
每日 19:00 拉取订单 ←────────────────────────── GET /orders?date=次日
      ↓
  制单（写入本地 SQLite server_orders 表）
      ↓
次日 09:10 启动脚本，检查 QMT 连接
次日 09:15 实时查询 QMT 真实资金/持仓，风控通过则下单
次日 15:30 查询全天成交（回到顶部）
```

---

## 每日时间线

| 时间 | 执行方 | 动作 |
|------|--------|------|
| 15:30 | 本地 | 查询全天最终成交 → POST /trade-result |
| 15:35 | 本地 | 下载当日股票 + 指数 + ETF 行情，清洗 → POST /market-data |
| 15:35+ | 服务器 | 行情入库（按标的）；根据成交回报更新所有受影响的策略虚拟状态 |
| 15:40 | 服务器 | 触发策略引擎：多实例运行，每实例查询自己虚拟状态及行情，生成原始信号 |
| 15:40~ | 服务器 | 原始信号 → 预检查（虚拟资金/持仓/手数）→ 归集 → 订单队列 |
| 19:00 | 本地 | 轮询 GET /orders?date=次日，写入本地 SQLite，推送微信 |
| 次日 09:10 | 本地 | Windows 任务计划触发下单脚本，检查 QMT 连接 |
| 次日 09:15 | 本地 | 从本地 SQLite 读取当日订单；实时查询 QMT 资金/持仓；风控通过则下单 |

---

## 数据存储设计（服务器）

### 1. 行情存储（Parquet，按标的）

根目录：`/data/market/daily/`，分三个子目录。

#### 股票日线（`/data/market/daily/stocks/`）

每只股票一个文件，如 `600519.SH.parquet`，按 `trade_date` 升序。

| 字段 | 类型 | 说明 |
|------|------|------|
| `trade_date` | int (YYYYMMDD) | 交易日，主键 |
| `open` | float | 开盘价 |
| `high` | float | 最高价 |
| `low` | float | 最低价 |
| `close` | float | 收盘价 |
| `volume` | int | 成交量（股） |
| `amount` | float | 成交额（元） |
| `turnover_rate` | float | 换手率（0~1） |
| `is_suspended` | bool | 是否停牌 |

#### 指数日线（`/data/market/daily/indexes/`）

每个指数一个文件，如 `000300.SH.parquet`。

| 字段 | 类型 | 说明 |
|------|------|------|
| `trade_date` | int (YYYYMMDD) | 交易日，主键 |
| `open` | float | 开盘点位 |
| `high` | float | 最高点位 |
| `low` | float | 最低点位 |
| `close` | float | 收盘点位 |
| `volume` | int | 成交量（部分指数不提供时填 0） |
| `amount` | float | 成交额（部分指数不提供时填 0） |

#### ETF 日线（`/data/market/daily/etfs/`）

每个 ETF 一个文件，如 `510300.SH.parquet`。字段与股票日线相同（含 `turnover_rate` 和 `is_suspended`）。

### 2. 策略虚拟状态表

| 字段 | 类型 | 说明 |
|------|------|------|
| `instance_id` | string PK | `{account_group}_{strategy_id}` |
| `virtual_cash` | float | 当前虚拟可用资金（元） |
| `virtual_positions` | json | 例如 `{"600519.SH": 100, "000001.SZ": 200}` |
| `last_update` | datetime | 最后更新时间（来自成交回报处理） |

### 3. 原始信号表（临时队列）

| 字段 | 类型 | 说明 |
|------|------|------|
| `signal_id` | string PK | UUID |
| `instance_id` | string | 外键关联策略实例 |
| `symbol` | string | 标的 |
| `direction` | string | BUY / SELL |
| `quantity` | int | 需求数量（股，策略保证为 100 的整数倍） |
| `reference_price` | float | 参考价（如当日收盘价） |
| `price_offset` | float | 超价幅度（如 0.005） |
| `limit_price` | float | `reference_price × (1 + price_offset)` |
| `valid_date` | int | 有效交易日 YYYYMMDD |
| `signal_time` | datetime | 生成时间 |
| `precheck_status` | string | PASS / FAIL |
| `precheck_reason` | string | FAIL 时的原因说明 |

### 4. 订单表

| 字段 | 类型 | 说明 |
|------|------|------|
| `order_id` | string PK | UUID |
| `account_group` | string | 真实账户组标识（如 real_A） |
| `symbol` | string | 标的 |
| `direction` | string | BUY / SELL |
| `quantity` | int | 归集后总数量（股） |
| `limit_price` | float | 最终限价（BUY 取最高，SELL 取最低） |
| `valid_date` | int | 有效交易日 YYYYMMDD |
| `status` | string | PENDING / FILLED / PARTIAL / EXPIRED |
| `created_at` | datetime | 订单生成时间 |

### 5. 订单→信号映射表

| 字段 | 类型 | 说明 |
|------|------|------|
| `order_id` | string | 订单 ID |
| `signal_id` | string | 原始信号 ID |
| `signal_quantity` | int | 该信号的数量（用于按比例拆分成交） |

### 6. 策略绩效表

| 字段 | 类型 | 说明 |
|------|------|------|
| `instance_id` | string | 策略实例 |
| `date` | int | 日期（YYYYMMDD） |
| `nav` | float | 当日净值（虚拟现金 + 虚拟持仓市值） |
| `daily_return` | float | 日收益率 |
| `positions_snapshot` | json | JSON 持仓快照 |

---

## 本地数据库（SQLite）

三张表关联关系：`server_orders.order_id` → `local_orders.order_id` → `trades.local_order_id`。

### server_orders 表（从服务器拉取的归集订单）

| 字段 | 类型 | 说明 |
|------|------|------|
| `order_id` | TEXT PRIMARY KEY | 服务器订单 ID |
| `account_group` | TEXT | 账户组 |
| `symbol` | TEXT | 标的 |
| `direction` | TEXT | BUY / SELL |
| `quantity` | INTEGER | 委托数量（股） |
| `limit_price` | REAL | 限价 |
| `valid_date` | TEXT | 有效交易日（YYYYMMDD） |
| `fetched_at` | TEXT | 本地拉取时间（ISO 8601） |

### local_orders 表（本地提交给 QMT 的委托记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| `local_order_id` | TEXT PRIMARY KEY | QMT 返回的委托编号 |
| `order_id` | TEXT | 服务器订单 ID（外键） |
| `account_group` | TEXT | 账户组 |
| `qmt_account_id` | TEXT | 实际使用的 QMT 账户 ID |
| `symbol` | TEXT | 标的 |
| `direction` | TEXT | BUY / SELL |
| `submitted_price` | REAL | 实际提交的限价 |
| `submitted_quantity` | INTEGER | 委托数量（股） |
| `submitted_at` | TEXT | 提交时间（ISO 8601） |
| `submit_status` | TEXT | SUCCESS / FAILED |
| `fail_reason` | TEXT | FAILED 时的原因（资金不足、持仓不足等） |

### trades 表（实际成交记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT | 自增主键 |
| `local_order_id` | TEXT | 关联本地委托 |
| `order_id` | TEXT | 服务器订单 ID（冗余，方便对账） |
| `filled_quantity` | INTEGER | 成交数量（股） |
| `filled_price` | REAL | 成交均价（元） |
| `filled_time` | TEXT | 成交时间（ISO 8601） |
| `status` | TEXT | FILLED / PARTIAL / CANCELLED / REJECTED |
| `reported_at` | TEXT | 推送给服务器的时间（ISO 8601） |

---

## 服务器端模块详解

### 模块一：行情入库（含指数和 ETF）

- 接口：POST /market-data
- 请求体包含三个数组：stocks、indexes、etfs
- 处理：对每个标的，读取对应 Parquet 文件，检查 trade_date 是否已存在，若不存在则追加一行，按日期排序后写回
- 记录最后成功交易日到元数据

### 模块二：成交回报处理

- 接口：POST /trade-result（仅 stage=close）

处理流程：

1. 根据 `order_id` 查询映射表，得到 `[(signal_id, signal_quantity), ...]`
2. 按 `signal_quantity` 比例将实际成交数量拆分到每个信号（最小单位 1 股，使用最大余数法；余量归入数量最大的信号）
3. 对每个原始信号，根据 `instance_id` 更新虚拟状态：
   - BUY：虚拟现金减少 `成交股数 × 成交价`，虚拟持仓增加成交股数
   - SELL：虚拟现金增加 `成交股数 × 成交价`，虚拟持仓减少成交股数
4. 更新订单状态为 FILLED 或 PARTIAL
5. 触发绩效快照（当日行情和成交均处理完毕后统一计算）

### 模块三：策略引擎（多实例）

配置从 `strategies.yaml` 读取，每个策略实例独立运行，注入 context 对象：

- `get_virtual_cash()` → float
- `get_virtual_position(symbol)` → int
- `get_market_data(symbol, start_date, end_date, fields)` → DataFrame

策略输出 RawSignal 对象：`instance_id, symbol, direction, quantity, reference_price, price_offset, valid_date`

### 模块四：预检查（虚拟资金/持仓）

对每个原始信号逐一检查：

| 检查项 | BUY | SELL |
|--------|-----|------|
| 手数规则 | quantity > 0 且为 100 的整数倍 | quantity > 0；若 quantity < virtual_position[symbol] 则必须为 100 的整数倍（清仓尾单除外） |
| 资金/持仓 | `quantity × limit_price × (1 + fee)` ≤ `virtual_cash` | `quantity` ≤ `virtual_positions.get(symbol, 0)` |

检查未通过：标记 `precheck_status=FAIL`，记录原因，触发微信告警，不进入归集。

### 模块五：归集引擎

1. 筛选 `precheck_status=PASS` 的信号
2. 按 `(account_group, symbol, direction, valid_date)` 分组
3. 合并规则：quantity 求和；limit_price 取 BUY 最高值、SELL 最低值
4. 生成 `order_id`（UUID），写入订单表，初始状态 PENDING
5. 写入映射表：`(order_id, signal_id, signal_quantity)` 每条信号一行

### 模块六：订单队列与 API

- 订单按 `valid_date` 索引
- 接口：GET /orders?date=YYYYMMDD，返回该日期所有 PENDING 订单

### 模块七：策略绩效与净值

每日成交回报和行情均处理完毕后：

1. 获取每个实例的 `virtual_cash` 和 `virtual_positions`
2. 从行情存储获取当日收盘价
3. 计算净值 = `virtual_cash + Σ(持仓 × 收盘价)`
4. 写入绩效表

提供查询接口：`GET /nav/{instance_id}?start=YYYYMMDD&end=YYYYMMDD`

---

## 本地模块详解

### 模块一：行情下载与推送（15:35）

- 触发时间：每个交易日 15:35（行情稳定后）
- 下载范围：
  - **股票**：全市场 A 股
  - **指数**：沪深300（000300.SH）、中证500（000905.SH）、中证1000（000852.SH）、上证50（000016.SH）、上证指数（000001.SH）、深证成指（399001.SZ）、创业板指（399006.SZ）等
  - **ETF**：与策略相关的 ETF 品种（由策略方确认清单）
- 下载：使用 `download_history_data2` 批量下载，`time.sleep(3)` 后用 `get_market_data_ex` 读取
- 清洗后 POST /market-data
- 失败处理：重试 3 次，仍失败则微信报警

### 模块二：成交查询与推送（15:30）

- 触发时间：每个交易日 15:30（先于行情下载触发）
- 调用 `XtTrade.query_stock_trades()` 获取当日所有成交
- 按 `order_id` 聚合（一个订单可能有多笔成交），计算成交均价
- POST /trade-result，携带 `order_id`、`filled_quantity`、`filled_price`、`status`

### 模块三：订单拉取与制单（19:00）

- 触发时间：每个交易日 19:00
- 接口：GET /orders?date=次日日期

**三态处理逻辑：**

| 状态 | 条件 | 本地处理 |
|------|------|---------|
| 有订单 | code=0，orders 非空 | 写入本地 SQLite server_orders 表，微信通知"已制单 N 条" |
| 无订单 | code=0，orders 为空 | 微信提示"今日无交易机会"（非报警） |
| 策略未完成 | code=3002 | 等待 30 分钟后重试一次；重试仍 3002 → 微信报警 |
| 服务器无响应 | 网络超时/连接拒绝 | 微信报警，等待人工介入 |
| 其他非 0 错误码 | 非预期响应 | 微信报警，等待人工介入 |

写入前校验：`valid_date` 与次日日期匹配，过期订单不写入。

### 模块四：竞价下单（次日 09:10）

- 触发时间：次日 09:10（Windows 任务计划程序自动触发）
- 从本地 SQLite `server_orders` 读取 `valid_date = 当日` 的订单
- 按 `account_group` 从本地配置文件查找对应的 `qmt_account_id`

**逐订单风控检查：**

| 订单类型 | 检查条件 | 通过 | 不通过 |
|---------|---------|------|-------|
| BUY | `quantity × limit_price ≤ 账户可用资金` | 调用 QMT passorder 提交限价单（FIX_PRICE） | 微信报警，写入 FAILED，不上报服务器 |
| SELL | `quantity ≤ 持仓可用数量` | 调用 QMT passorder 提交限价单（FIX_PRICE） | 微信报警，写入 FAILED，不上报服务器 |

委托结果写入本地 `local_orders` 表。模拟盘统一使用限价单，不支持市价单。

---

## 通知机制

工具：企业微信机器人 Webhook（或个人微信第三方推送服务）

| 触发场景 | 类型 |
|---------|------|
| 每日 19:00 有订单 | 提示（含订单数量摘要） |
| 每日 19:00 无订单 | 提示（正常情况，非报警） |
| 订单拉取 3002 且重试仍失败 | 报警 |
| 服务器无响应（超时/连接拒绝） | 报警 |
| 服务器预检查 FAIL（虚拟资金/持仓/手数不符） | 报警（含策略实例和标的信息） |
| 本地下单前风控失败 | 报警（含账户、标的、资金/持仓信息） |
| 行情推送失败（重试后） | 报警 |
| 策略绩效偏离检测触发 | 报警（含偏离策略和阈值） |

---

## 开发顺序建议

1. 服务器基础环境（Ubuntu + Python + Parquet 读写）
2. 行情入库模块（股票 + 指数 + ETF）
3. 虚拟状态表设计与初始化（从 strategies.yaml 读取）
4. 成交回报处理 + 拆分逻辑 + 虚拟状态更新
5. 策略框架（多实例 + context 注入 + 信号输出）
6. 预检查 + 归集 + 订单队列 + GET /orders API
7. 本地模块适配（行情推送含指数ETF、成交回报推送、订单拉取、本地风控下单）
8. 净值计算与绩效跟踪
9. 偏离检测与微信报警接入

---

## 关联文档

- `v2/API接口文档（纯股）v2.1.md`：服务器端 API 接口规范，供搭档使用
- `项目设计文档（含期权）.md`：扩展版，股票流程验证完成后参考
- `C:/parttime/qmt数据推送/v3/CLAUDE.md`：历史开发错误复盘，开发时必读
