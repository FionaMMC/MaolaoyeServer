# V2 开发注意事项

记录 v2 开发过程中的关键决策和潜在陷阱。出问题时优先检查本文件，其次检查 v1/开发注意事项.md（v1 的经验大多仍适用）。

---

## 注意一：日志使用 logging 模块而非 print

### 决策
所有模块使用 `config.setup_logger(name)` 获取 logger，不使用 print。

### 使用方式
```python
import config
log = config.setup_logger("market_push")   # 在脚本顶层调用一次

log.info("正常流程")
log.warning("异常但非致命")
log.error("需要人工介入")
```

### 日志位置
`v2/logs/{name}.log`，按天滚动，保留 30 天。同时输出到控制台。

### 与微信告警的关系
- `log.error(...)` 对应需要微信报警的场景（同时调用 `_wechat_alert()`）
- `log.warning(...)` 对应需要关注但不报警的场景
- `log.info(...)` 对应正常流程记录

---

## 注意二：PUSH_MODE 双模式设计

### 决策
`config.PUSH_MODE = "local" | "server"` 控制所有服务器交互：
- `"local"`：POST 请求写入 `local_push/` 文件夹；GET /orders 读 `orders_mock.json`
- `"server"`：走真实网络

### 适用范围
覆盖所有模块：market_push.py（POST）、trade_result.py（POST）、order_query.py（GET）。
切换模式只需改 config.py 一处，不改业务代码。

### local 模式的 mock 文件
`order_query.py` 在 local 模式下读取 `local_push/orders_mock.json`。
文件格式与服务器真实响应相同，参考 `local_push/orders_mock_template.json`。
若文件不存在，视为"今日无订单"，正常退出，不报警。

---

## 注意三：换手率（turnoverRatio）不推送

### 决策
`POST /market-data` 的 stocks[]、etfs[] 中不推送 `turnover_rate` 字段。

### 原因
经查 XtQuant.XtData 文档，日线 K线字段列表（get_market_data_ex period="1d"）中不含 turnoverRatio。
API 接口文档已将此字段标注为可选（"否"），服务器不依赖此字段。
v1 pipeline 同样未推送此字段。

### 后续
若服务器策略需要换手率，届时通过 `get_instrument_detail()` 或其他途径单独获取。

---

## 注意四：suspendFlag 字段的使用

### 决策
通过 `suspendFlag` 字段判断是否停牌，不通过 volume==0 推断。

### 字段含义（来自 XtQuant 文档）
- `0`：正常交易
- `1`：停牌
- `-1`：当日起复牌（可以交易，`is_suspended=False`）

### 代码逻辑
```python
is_suspended = int(row.get("suspendFlag", 0)) == 1
```

---

## 注意五：stocks / ETF / indexes 的下载和读取策略

### 决策
三类标的合并为一个列表，调用一次 `download_history_data2`；读取时分三次调用 `get_market_data_ex`（stocks+ETFs 用 `dividend_type="front"`，indexes 用 `dividend_type="none"`）。

### 原因
- 一次 download 比三次更高效
- 读取分开是因为 indexes 没有分红，显式用 "none" 更准确
- stocks 和 ETFs 都有除权/分红场景，用 "front" 前复权

### 首次运行注意
首次运行时 download_history_data2 会下载全量历史数据，可能耗时较长（数小时）。
后续每日增量下载应在几分钟内完成。

---

## 注意六：板块名称需第一次运行时验证

### 决策
`config.py` 中配置：
```python
STOCK_SECTOR  = "沪深A股"
ETF_SECTORS   = ["上证基金", "深证基金"]
INDEX_SECTORS = ["上证指数", "深证指数"]
```

### 验证方式
`startup_check()` 会打印各板块实际返回数量。若某板块返回 0，说明板块名错误。
可通过以下代码查看所有可用板块名：
```python
from xtquant import xtdata
xtdata.data_dir = r"C:\parttime\平安证券量盈QMT策略交易平台\userdata_mini"
print(xtdata.get_sector_list())
```

### startup_check 的保护
若 STOCK_SECTOR 返回数量少于 100，直接 raise AssertionError，阻止执行，避免推送空数据到服务器。

---

## 注意七：get_market_data_ex 返回值结构

### 决策
读取时总使用 `fill_data=False`，避免停牌股票的数据被向后填充。

### 返回格式
```python
raw = xtdata.get_market_data_ex(...)
# raw 是 dict: { "600519.SH": DataFrame, "000001.SZ": DataFrame, ... }
# 每个 DataFrame：index=时间戳, columns=["open","high","low","close","volume","amount","suspendFlag"]
# 请求 start_time=end_time=today，每只股票应返回只含当日的 DataFrame
```

### 提取当日数据
```python
row = df.iloc[-1]   # 当日（也是唯一）一行
```

若 df.empty 或 row.isna().all()，跳过该标的。

---

## 注意八：order_submit.py 的多账户 QMT 连接

### 待验证（开发 order_submit.py 时确认）
v2 支持多账户。当有多个 account_group 时，是否能在一个 XtQuantTrader 会话中订阅多个 StockAccount 并分别下单？
预期写法：
```python
acc_A = StockAccount("1234567890")
acc_B = StockAccount("0987654321")
xt_trader.subscribe(acc_A)
xt_trader.subscribe(acc_B)
```
若不支持，则需为每个账户创建独立的 XtQuantTrader 会话（不同 session_id）。
**第一次开发 order_submit.py 时必须验证此行为，不得假设。**

---

## 继承自 v1 的注意事项

以下规则在 v2 中完全继承，详见 `v1/开发注意事项.md`：

- **注意一**：download_history_data2 + get_market_data_ex 必须配对（不得混用非 ex 版）
- **注意二**：download 后必须 sleep(3) 再读取
- **注意三**：前复权（dividend_type="front"）用于股票和 ETF
- **注意四**：download_history_data2 的 start_time="" 触发增量下载
- **注意八**：次日交易日查询范围向后 14 天覆盖长假
- **注意九**：valid_date 校验是强制过滤
- **注意十**：3002 重试语义（等待 30 分钟，仅重试一次）
- **注意十一**：order_query.py 中 xtdata 只用于交易日历，不需要 QMT 在线
- **注意十二**：server_orders 表使用 INSERT OR IGNORE 保证幂等（对应 v1 的 signals 表）
- **注意十四**：order_stock() 返回负数代表失败
- **注意十七**：traded_time 格式需第一次联调时验证
