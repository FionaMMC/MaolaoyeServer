# V7.13 任务 1：Hydra 审计

结论：**合成契约测试通过，真实产物验收阻塞，禁止向下一步发布。**

已确认：

- walk-forward 审计逻辑要求 `label_end <= as_of_date`；
- 合成 2026-06-30 输入输出权重和为 1；
- `1-3Y` 映射为 `511160.SH`，生效日为 `2025-01-06`；
- manifest 含 asset weights、signals、training audit、proxy map 四个 SHA-256；
- 删除所选久期映射后脚本 fail closed，没有回退到旧 10 年 ETF。

阻塞证据：本机冻结检出没有真实 signals/training-audit/债券数据，且 publisher 不校验执行 ETF 行情文件和最后交易日。因此没有生成 `v713_hydra_target_20260630.parquet`，也没有把合成测试当作真实产物。

修复要求：加入版本化 ETF market manifest（每代码 last trade date、source、file hash），并在 publisher 中按 `as_of_date` fail closed 校验。
