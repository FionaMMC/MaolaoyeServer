# V7.13 任务 3：服务器回放与模拟盘审计

结论：**本地自动化回放通过；隔离模拟盘未执行；不批准下单。**

- V7.13 relay 定向测试覆盖有效篮子、篡改、非法 sleeve、dry-run 重放幂等和 server guard 阻断。
- 全服务器测试通过（唯一 skip 为既有 V53 真实 bundle 缺失）。
- 没有经过复核的真实 `v713_target_latest.parquet`，没有专用 QMT 模拟账户，未产生生产订单/成交/NAV 日志或截图。
- 默认配置明确禁止订单，不能把这个状态描述为“模拟盘已部署”。

上线前按 `docs/V713_DEPLOYMENT_AND_ROLLBACK.md` 完成专用账户、离线 replay、首日成交和对账验收。
