# V7.13 任务 2：主篮子审计

结论：**代码测试通过，真实固定输入回放阻塞。**

- 冻结提交：`25385546c686788d1becdf804863ebe509e11243`。
- 单元测试 `4 passed`：拒绝收益序列、权重归一、TOP50 产物、TOP50 不读取无用 Hydra。
- 代码输出 Parquet + JSON，`strategy_version=v7.13-base`，sleeve 仅 `TOP50/T1_5050/AUX_HYDRA`，basket hash 使用稳定排序和固定浮点格式。
- 未生成真实 2026-07-01 篮子：上游真实 Hydra 产物缺失。
- 未取得“两次真实运行同 hash”的证据；不能填写虚构 hash 或路径。

阻塞修复：主篮子需同时校验 Hydra manifest 的版本、ETF 代理状态、行情截止日和 manifest/hash 绑定。
