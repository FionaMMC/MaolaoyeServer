# V53 / 分红估值修复版上线回执 · 2026-09-21

## 已上线的是什么

用户在审计后明确要求推进服务器上线。本次部署应用代码：

- `c0bde0332bc9f8bf3ba5ed98af64aece808b78cb`
- GitHub 分支：`codex/hydra-reliability-dividend-audit-20260921`
- 上一应用版本：`78a309245f756cc55626a44ba8001ae9d87a7b9d`
- 生产服务启动：2026-09-21 **13:33:55 Asia/Shanghai**。

此回执是随后追加的文档；文档提交不会更换已验证的应用代码或再次触发调仓。

服务器现在具备 V53 新月目标保存 / 确认成交后的差额续做，以及独立应收股息估值、NAV 组成记录能力。既有 Hydra 实盘、模拟盘、影子盘入口保持原样，未启用新的策略版本或改变 50bp 执行政策。

## 验证结果

- 本地完整回归：874 passed、1 skipped（47.62 秒）。
- 服务器隔离工作区完整回归：874 passed、1 skipped（313.77 秒）。使用生产 Python 和原服务依赖，测试工具及缺少的客户端测试依赖来自已有隔离环境；没有安装或升级生产虚拟环境。
- 现网服务用户能加载候选模块。
- 生产数据库副本迁移：原有 26 张表行数及内容摘要全部相同，只新增 `dividend_entitlements` 与 `perf_valuations` 两张空表。
- 副本上的候选服务启动、dashboard、blueprint、V53 指标接口全部 HTTP 200，调度器在隔离测试中关闭。
- 实际上线后：active/running、NRestarts=0、healthz=ok、readyz=ready、dashboard 与 blueprint HTTP 200。
- 上线前后数据库再次逐表核验：原有 26 张表内容完全一致、新表为空；私有配置与策略配置摘要一致。

配置与 SQLite 一致性备份、逐表摘要和完整部署回执保存在服务器私有备份区；不把这些原始材料放入 GitHub。

## V53 九月故障：服务器能证明什么

进一步查服务器确认：九笔 20260901 订单首次取单时间均为 **2026-08-31 20:23:46 +08:00**；当前状态 EXPIRED；其 `trades` 回报记录和 `order_status_evidence` 记录都为零。

现网日志使用每日轮转、保留 14 份的设置。当前滚动日志不再覆盖 9/1，systemd journal 未找到该区间记录；已检查的备份 / 私有归档目录也未找到对应历史日志。现有 live QMT 备份接口保存的是行情，不是自动上传的 Windows 提交日志。

因此，可以确定**服务器未留存这些订单的终局成交回报**，但不能据此证明 QMT 完全没有下单或成交。Windows 提交 / 回报日志、券商历史委托成交或者额外留存的外部归档仍是恢复九月目标需要的证据。这项缺失不阻塞本次程序升级，但不允许盲目重发旧单。

## 本次没有做的事

1. 没有重跑九月调仓、重开旧单、创建新 target、下单、撤单或改动策略资金 / 持仓。
2. 没有把补丁安装到 Windows；模拟盘 GET 重试仍需同伴更新对应客户端文件后才会在其电脑生效。
3. 没有登记生产分红权益，也没有用今天持仓覆盖历史净值。**分红代码上线不代表旧报表假跌已经自动修正。** 登记日权益、实际到账匹配和历史逐日重算仍需完成。
4. 没有修改 V48 的旧远期利率信号或移除债券；没有发布新的 713 研究产物。相关审计问题仍见原报告。

另有既有告警：`Shadow_Hydra_V481_RB` 的旧 target 日期为 20260724，9/18 的影子盘任务因超过 freshness 范围退出失败。这是输入陈旧问题，不是 HTTP 服务宕机；本次没有通过延长期限来掩盖它，也没有清掉该告警。

## 后续顺序与回退原则

1. 同伴更新模拟盘客户端，并补 V53 九月的本地 / 券商历史事实；确认账本后再制定当月差额恢复。
2. 登记并核对分红权益与到账，重算历史；同步修复 V48 / 影子盘 / 713 的输入与回测口径。
3. 月末仍需 Windows 上传与服务器月度研究链的原生回执，不能用此次服务升级代替端到端验收。

如需回退，先核对是否已有新交易或权益登记，再做代码兼容回退；**不得用上线前数据库覆盖上线后的真实交易事实**。本次没有执行回退。
