# 策略增减资与 QMT 现金事实：第一阶段接口与迁移边界

日期：2026-09-08。本文件描述本次本地重构切片，不表示已在生产部署或实盘验收。

## 1. 两件不同的事，不能重复做账

| 事件 | 来源 | 当前处理 | 不会做什么 |
| --- | --- | --- | --- |
| 账户实际入金/出金/分红等已经发生 | QMT 明细和快照 | 记录账户事实，等待归属投影 | 不要求 server 再批准这笔事实；不自动全分给 Hydra |
| 给策略增资/减资 | 经营决策 | 在同账户内增量改变策略现金归属，写资本分录 | 不修改券商总余额；不生成订单；不覆盖策略本金、收益或持仓 |

例如账户入金 1,900 万，策略原来只有 21.1 万：先保存这笔真实入金事实，策略仍然管理原有 21.1 万及其盈亏。只有明确给策略 `INCREASE 50000`，策略所属资产才增 5 万。不能用“把余额设成 26.1 万”重置它已经产生的盈亏。

账户现金事实与策略资本分录进入**不同表**。否则一次真实入金与随后的一次策略增资会被当成两次外部流入，净值就会算错。

## 2. QMT 现金事实接口：事实先收下，交易与归属可以稍后

`POST /accounts/cash-observations`

```json
{
  "execution_domain": "live",
  "account_alias": "example-account",
  "source": "qmt-cash-ledger",
  "source_event_id": "broker-event-stable-id",
  "event_type": "DEPOSIT",
  "amount": "19000000.00",
  "currency": "CNY",
  "observed_at": "2026-09-08T09:00:00+08:00",
  "evidence_sha256": "<真实证据的64位小写SHA-256>",
  "qmt_cash_balance": "19211000.00",
  "qmt_available_cash": "19211000.00"
}
```

- 验证调用身份、执行域、账户归属和消息格式，不检查交易生成开关、旧 `live_cash_flow_ingest_enabled` 或策略是否存在。
- 同一域、账户、来源、来源事件 ID 只保留一份不可变事实；原消息重试返回同一 ID，内容不同不能覆盖，必须另发更正事件。
- 原始证据摘要与完整规范化消息摘要分别保存，原始输入载荷也持久保存。
- 返回 `RECORDED_UNALLOCATED`、`strategy_balance_changed: false`。这表示“事实收下了，尚未把金额投影到具体策略”，不是“再次等待批准入金”。
- 即使账户出现负余额观察、归属还没解释通，仍可保存事实；不会为了让 server 数字好看而拒绝已有事实。
- `DIVIDEND / INTEREST / FEE / OTHER` 同样只是事实留存，当前还不自动完成按持仓归属的分红/费用投影。
- 已收到但未投影归属的正数 `DIVIDEND / INTEREST / OTHER` 会作为待归属金额，从可增资的未分配池中扣除，不会误当成某策略能领走的新本金；账户其他原有未分配余额仍可正常增资。普通 `DEPOSIT` 不属于这类收入悬账，`WITHDRAWAL` 已体现在 QMT 余额中，不重复扣减。
- 该接口不联系 QMT。来源真实性来自已配置的账户范围凭据和提交端的实际 QMT 采集，SHA-256 本身不证明来源，也不代替传输认证。

## 3. 策略资本接口：只有增资和减资，没有直接设余额

`POST /accounts/capital-movements`

```json
{
  "execution_domain": "live",
  "account_alias": "example-account",
  "instance_id": "live_hydra_v481_rb",
  "action": "INCREASE",
  "amount": "50000.00",
  "event_date": "20260908",
  "qmt_cash_balance": "19211000.00",
  "snapshot_time": "2026-09-08T15:30:00+08:00",
  "source_event_id": "hydra-capital-increase-stage-02",
  "evidence_sha256": "<真实证据的64位小写SHA-256>",
  "description": "第二阶段增资；后续观测实际滑点"
}
```

`DECREASE` 也使用正数 `amount`；服务端写负的 `CAPITAL_DEALLOCATION`。支持 CNY 分级金额，禁止 `SET_BALANCE` 和额外余额覆盖字段。

本接口是立即生效命令，不是预约或回溯改账。请求 `event_date` 只作为计划日期保存在原始回执；真实资本分录使用服务端执行时的上海日期，并在 `effective_date` 返回。否则今日已增资、却把分录写到未来，会让今日收益被虚增。

保留 G08/G09 的实际意义：只能使用已初始化且明确域、账户、策略归属的 attributed 子账本。该接口不兼做初始化或切换账本模式，不重置持仓和策略状态。

### 增资

策略现金增加指定金额，所有策略归属现金之和不能超过所提供的 QMT **总现金余额（包含冻结现金）**。其他策略的负账面余额不能充当本策略可申请的空闲资金。缺少账户归属的旧账本应先迁移归属，不能当成不存在。

若同账户已有成交账务差异，特别是某策略卖出所得还没记回它，不能把这一缺口认作其他策略可取的未分配资金。只暂停本次新增资本归属，先接回原事实；不阻塞继续收事实或恢复旧订单。

快照不得早于同一账户服务器已经收到的最新成交、账户现金事实或旧接口真实现金流。比较使用带时区的真实时刻，不按字符串大小。若之后发生过买入/出金，旧快照不能拿来重复计算“还剩多少未分配现金”；仅本次新增资本归属要求重新读取 QMT，事实接收不受影响。内部增减资不是券商现金变化，不会让同一份物理快照失效；成功事件重放也先返回原回执，不重走此检查。旧 `CAPITAL_ALLOCATION` 复用同一因果时间检查。

这里必须区分：

- `cash_balance`：账面真实现金总额，包含被冻结但还没成交的钱，用于归属计算；
- `available_cash`：柜台此刻实际可买的钱，用于下单队列；
- `reserved/obligation`：已有订单仍可能用掉的钱，不能被第二次消费。

这次增资**不会改写已经冻结的 target、批次、订单数量**。响应的 `NEXT_UNFROZEN_TARGET` 表示只能供下一次未冻结目标使用，不是自动触发目标重算的任务。当前月度 residual 仍追踪原已冻结目标；如果希望本月增资马上投入，需显式生成一轮新的资金重平衡，不能偷偷扩大补单。

### 减资

只可减少“本策略现金－本策略未终局买单保护金额”。无须等待全账户、所有策略都空闲；但不能拿别人的资金，也不能默默撤销旧单。

- 保护额按剩余股数、限价、费用预算计算，部分成交使用累计成交量的最大值，不能把多次累计回报重复相加。
- `FILLED / CANCELLED / REJECTED / NOT_SUBMITTED` 才属于当前兼容层已知的释放依据。新的“超过时间、业务 close”不修改 broker 事实，也不自动释放这部分钱。
- `NOT_SUBMITTED` 必须由提交端证明未调用券商下单并以零成交回报；不是未知委托的别名。
- 同一策略存在“成交已发生但账没接回”的差异标记时，先接回事实再允许取走资金。别的策略明确归属的正常未决单不应阻止本策略取其自由现金。
- 只保护 cash；如果可减现金不足，不会自动卖出持仓。经营上可另行明确批准卖出筹资计划，等待真实可用现金回流，再执行减资。`reduce-only` 只是“只能减仓、不增加敞口”的可选政策概念，本次没有新增该模式或开关，也不把它当普通交易、增减资的前置条件。
- 现阶段费用保护是保守上界：部分成交已扣的最低费用可能暂时再次预留，待订单终结释放。没有假装已经实现逐笔 broker 冻结分摊。
- 旧聚合单可能包含多个策略，旧结算逐次拆分时的整数舍入使简单按比例估算剩余义务并不精确。当前仅在这类共享订单的减资判断中，给相关策略保留整个订单的剩余上界；未来应改为持久化累计的逐策略分配，再按准确归属释放。这是明确的兼容限制，不声称多策略共享单的精确 reserve 已全部完成。

旧 `/cash-flows` 的 `CAPITAL_DEALLOCATION`（包括 attributed 迁移）现在也调用同一未决买单保护，不能从旧接口绕过。旧 `CAPITAL_*` 接口仍保留历史 `qmt_cash=available` 语义，可能偏保守；新调用请转到本接口，不能把新字段含义套进旧字段。

## 4. 幂等、并发、收益口径

- 每个资本事件在 `cash_flow_journal` 增加一个分录，在 `capital_movement_receipts` 增加对应请求与计算快照，同一事务提交。
- 重试先找原始回执，再看当前资源，所以“已经增资但 HTTP 回包丢失”不会第二次加钱，也不会因为稍后现金变了而把原成功说成失败。
- `source_event_id` 必须稳定。两次不同金额的计划使用不同 ID；网络重试必须原样使用旧 ID。
- SQLite 的参与账本写事务在读余额前使用 `BEGIN IMMEDIATE`，不持锁等待成交或网络。新资本 API 与现金事实 API 使用同步 FastAPI endpoint，在工作线程执行数据库调用，不堵塞事件循环。
- 本切片与原现金流写入共享短事务边界；其他账本写者的接入以主任务最终集成为准。非 SQLite 只锁已有账户行，还没有独立账户锁行，不能宣称已经支持任意多实例数据库部署。
- 策略现金字段继续兼容旧 Float 存储；输入增减资为 Decimal/分，但这不是完整 Decimal 总账迁移。
- `PerfService` 排除快照区间内已入账的 `DEPOSIT / WITHDRAWAL / CAPITAL_ALLOCATION / CAPITAL_DEALLOCATION` 后计算收益；分红仍是收益，不排除。
- 当前收益采用**期末现金流假设**：`(NAV今－NAV前－外部净流入)/NAV前`，不是任意时刻流入的精确 TWR。历史快照不会自动全量重算，迟到资金事件、盘中资金收益应由后续份额净值/TWR 与重述任务解决。

## 5. 什么时候逐步增资

经营规则建议为“明确金额的多阶段增资”，不是按账户总余额自动放大：

1. 当前一轮订单、实际成交、未成交尾单和费用归属能解释清楚；滑点数据能绑定策略、标的、下单时点和真实成交。
2. 使用现有规模作为观察基线，记录真实滑点、成交率、等待时间、佣金、ETF 溢价；分开买卖和标的，不只看全账户平均。
3. 下一阶段批准一个**增资金额**，在形成新冻结目标前执行分录；保持旧订单和已有本金/盈亏不变。
4. 比较同一市场条件下不同订单名义金额、成交参与率的变化。不能把市场波动或样本太少误当容量结论。
5. 若滑点或未成交率明显恶化，停止下一阶段增资；如需减资且现金不够，另行批准卖出筹资计划，再按实际自由现金执行减资分录。这里不新增自动减仓或 `reduce-only` 模式。

没有在代码里编造“每阶段必增多少、跑几天就必定安全、多少 bps 一刀切”的阈值，也没有启用自动增资。阈值要以真实样本、用户资金计划、标的流动性确定。

## 6. 上线前必须承认的迁移边界

本切片没有伪装成完整账户总账：

- 账户现金观察仍是追加事实库，还没有完整“未分配池＋策略子账＋悬账＋应收应付”的双分录账户投影；`RECORDED_UNALLOCATED` 不等于自动到账某策略。
- 待归属正收入的排除目前没有“已投影消费”标记，因而会保守保留，直到显式的事实到策略投影迁移。不能把同一股息手工通过旧接口再贷记策略之后，就假定新的排除已自动消失；迁移必须建立一对一关联并防重复计账、重复排除。本次没有自动贷记或静默释放此悬账。
- 资本接口使用提交的 QMT 快照，并未联网重新获取它。已实现“不能早于服务器已收资金/成交事实”的因果水位，但这不是完整柜台快照同步协议：未上传的手动交易、尚未收到的成交、账户外部挂单仍可能使可分资金判断过时。正式自动增资前必须把即时采集和所有账户写者接入客户端/账户执行器，不能仅靠本 API 宣称安全。
- “总现金足够”并不表示“此刻可买现金足够”。真实下单仍必须查 QMT 可用现金，先卖成交再买，不承诺卖出金额已经到账。
- 暂未实现自动逐步增资调度、减资清仓队列、新资金重平衡触发、精确 broker 冻结分配、多策略通用费用/分红分摊、精确 TWR。
- 新表是新增建表，不改旧表列。但生产仍应先备份数据库，在副本跑建表与重放核验，再对实际版本做部署演练；本任务未连接真实 QMT、未登记真实现金流、未提交或撤销任何订单。
- 旧初始化/策略迁移流程仍独立保留 G08。现网客户端不应继续把真实外部入金当成 Hydra 专属 `DEPOSIT`；迁移后应先发账户事实，再按明确增减资命令变更归属，避免一笔钱重复入账。
